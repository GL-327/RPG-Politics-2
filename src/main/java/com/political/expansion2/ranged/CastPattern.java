package com.political.expansion2.ranged;

/** Execution template used by {@link RangedAbilityEngine2}. */
public enum CastPattern {
    BEAM, CONE, AROUND, AIM, DOMAIN, CHAIN, PULL,
    HEAL, HOLY_NOVA, SANCTUM, SUMMON
}
