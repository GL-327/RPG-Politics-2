package com.political;

import eu.pb4.sgui.api.elements.GuiElementBuilder;
import eu.pb4.sgui.api.gui.SimpleGui;
import net.minecraft.component.DataComponentTypes;
import net.minecraft.item.ItemStack;
import net.minecraft.item.Items;
import net.minecraft.screen.ScreenHandlerType;
import net.minecraft.server.network.ServerPlayerEntity;
import net.minecraft.text.Text;
import net.minecraft.util.Formatting;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;

public class CustomItemsGui {

    // ════════════════════════════════════════════════════════════
    // MAIN MENU
    // ════════════════════════════════════════════════════════════

    public static void open(ServerPlayerEntity player) {
        SimpleGui gui = new SimpleGui(ScreenHandlerType.GENERIC_9X6, player, false);
        gui.setTitle(Text.literal("✨ Custom Collection"));

        // Decorative background with symmetric pattern
        for (int i = 0; i < 54; i++) {
            if (i % 9 == 0 || i % 9 == 8 || i < 9 || i >= 45) {
                gui.setSlot(i, new GuiElementBuilder(Items.PURPLE_STAINED_GLASS_PANE)
                        .setName(Text.literal("⬛").formatted(Formatting.DARK_PURPLE))
                        .build());
            } else {
                gui.setSlot(i, new GuiElementBuilder(Items.BLACK_STAINED_GLASS_PANE)
                        .setName(Text.literal("")).build());
            }
        }

        // Center header
        gui.setSlot(4, new GuiElementBuilder(Items.NETHER_STAR)
                .setName(Text.literal("🌟 Custom Collection").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎮 Browse exclusive items").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("💎 Premium gear & tools").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⚔️ Unique weapons & armor").formatted(Formatting.GRAY))
                .glow()
                .build());

        // Top row - Weapon categories (symmetric layout)
        gui.setSlot(10, new GuiElementBuilder(Items.IRON_SWORD)
                .setName(Text.literal("⚔️ Bounty Swords (T1)").formatted(Formatting.YELLOW, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🗡️ Tier 1 bounty weapons").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🎯 7 slayer types").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openSwordsMenu(player))
                .build());

        gui.setSlot(11, new GuiElementBuilder(Items.DIAMOND_SWORD)
                .setName(Text.literal("⚔️ Bounty Swords (T2)").formatted(Formatting.AQUA, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🗡️ Tier 2 bounty weapons").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🎯 Upgraded versions").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openSwordsMenu(player))
                .build());

        gui.setSlot(12, new GuiElementBuilder(Items.BLAZE_ROD)
                .setName(Text.literal("⚡ HPEBM Weapons").formatted(Formatting.LIGHT_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("⚡ Energy beam weapons").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🔋 All marks available").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openHPEBMMenu(player))
                .build());

        gui.setSlot(13, new GuiElementBuilder(Items.NETHERITE_SWORD)
                .setName(Text.literal("🗡️ Special Weapons").formatted(Formatting.GREEN, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎁 Unique special weapons").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🪄 The Gavel, Harvey's Stick").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openSpecialWeaponsMenu(player))
                .build());

        gui.setSlot(14, new GuiElementBuilder(Items.WITHER_SKELETON_SKULL)
                .setName(Text.literal("💀 Boss Drops").formatted(Formatting.DARK_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎁 Unique boss rewards").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("❤️ Undead Heart, 📻 Echoing Core").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openBossDropsMenu(player))
                .glow()
                .build());

        gui.setSlot(15, new GuiElementBuilder(Items.GOLD_BLOCK)
                .setName(Text.literal("🧱 Materials").formatted(Formatting.YELLOW, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🔨 Chunks & Cores").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("📦 All slayer types").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openMaterialsMenu(player))
                .build());

        gui.setSlot(16, new GuiElementBuilder(Items.GOLDEN_HELMET)
                .setName(Text.literal("👑 Piglin Collection").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("💰 Premium piglin items").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("👑 Crowns, Cores, Armor").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openPiglinMenu(player))
                .glow()
                .build());

        // Second row - Armor categories (symmetric layout)
        gui.setSlot(19, new GuiElementBuilder(Items.IRON_CHESTPLATE)
                .setName(Text.literal("🛡️ Bounty Armor (T1)").formatted(Formatting.GREEN, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🛡️ Tier 1 bounty armor").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🎯 7 armor sets").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openArmorMenu(player, 1))
                .build());

        gui.setSlot(20, new GuiElementBuilder(Items.DIAMOND_CHESTPLATE)
                .setName(Text.literal("🛡️ Bounty Armor (T2)").formatted(Formatting.DARK_AQUA, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🛡️ Tier 2 bounty armor").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("📈 Enhanced protection").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openArmorMenu(player, 2))
                .build());

        gui.setSlot(21, new GuiElementBuilder(Items.GOLDEN_CHESTPLATE)
                .setName(Text.literal("👑 Gold Armor Evolution").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("📈 5 tiers of gold armor").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⬆️ Pure → Polished → Gilded").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openGoldArmorMenu(player))
                .build());

        gui.setSlot(22, new GuiElementBuilder(Items.NETHERITE_HELMET)
                .setName(Text.literal("👑 Legendary Gear").formatted(Formatting.DARK_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🌟 Ultra-rare items").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("👑 Voidwalker's Crown").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openLegendaryMenu(player))
                .glow()
                .build());

        gui.setSlot(23, new GuiElementBuilder(Items.ENCHANTED_BOOK)
                .setName(Text.literal("✨ Weapon Attributes").formatted(Formatting.RED, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🔥 Weapon ability tokens").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⚡ 10 unique attributes").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openAttributeTokensMenu(player))
                .glow()
                .build());

        gui.setSlot(24, new GuiElementBuilder(Items.GOLD_NUGGET)
                .setName(Text.literal("💰 Compacted Materials").formatted(Formatting.YELLOW, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🧱 Dense material blocks").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⭐ Super compacted ores").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openCompactedMenu(player))
                .build());

        // Bottom navigation
        gui.setSlot(49, new GuiElementBuilder(Items.BARRIER)
                .setName(Text.literal("❌ Close").formatted(Formatting.DARK_RED, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("Exit collection").formatted(Formatting.GRAY))
                .setCallback((idx, type, action) -> player.closeHandledScreen())
                .build());

        gui.open();
    }

    // ════════════════════════════════════════════════════════════
    // HELPER METHODS - Placeholder implementations
    // ════════════════════════════════════════════════════════════

    private static void openHPEBMMenu(ServerPlayerEntity player) {
        // TODO: Implement HPEBM menu
        player.sendMessage(Text.literal("HPEBM menu coming soon!").formatted(Formatting.YELLOW), false);
    }

    private static void openCompactedMenu(ServerPlayerEntity player) {
        // TODO: Implement compacted materials menu
        player.sendMessage(Text.literal("Compacted materials menu coming soon!").formatted(Formatting.YELLOW), false);
    }
}
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🗡️ All T1 & T2 bounty swords").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("🎯 7 slayer types × 2 tiers").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openSwordsMenu(player))
                .build());

        // 🛡 T1 Bounty Armor
        gui.setSlot(15, new GuiElementBuilder(Items.LEATHER_CHESTPLATE)
                .setName(Text.literal("🛡️ Bounty Armor T1").formatted(Formatting.BLUE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎯 T1 armor sets for all slayer types").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⭐ Requires Bounty Level " + SlayerItems.T1_ARMOR_LEVEL_REQ).formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openArmorMenu(player, 1))
                .build());

        // 🛡 T2 Bounty Armor
        gui.setSlot(16, new GuiElementBuilder(Items.DIAMOND_CHESTPLATE)
                .setName(Text.literal("🛡️ Bounty Armor T2").formatted(Formatting.LIGHT_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎯 T2 armor sets for all slayer types").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⭐ Requires Bounty Level " + SlayerItems.T2_ARMOR_LEVEL_REQ).formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openArmorMenu(player, 2))
                .glow()
                .build());

        // 👑 Gold Armor Evolution
        gui.setSlot(20, new GuiElementBuilder(Items.GOLDEN_CHESTPLATE)
                .setName(Text.literal("👑 Gold Armor Evolution").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("📈 5 tiers of progressive gold armor").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⬆️ Pure → Polished → Shiny → Glistening → Gilded").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openGoldArmorMenu(player))
                .glow()
                .build());

        // ⭐ Legendary Gear
        gui.setSlot(22, new GuiElementBuilder(Items.NETHER_STAR)
                .setName(Text.literal("🌟 Legendary Collection").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("💎 Elite special equipment").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("• ⚔️ Berserker Helmet").formatted(Formatting.DARK_GREEN))
                .addLoreLine(Text.literal("• 🕷️ Venomous Crawler Leggings").formatted(Formatting.DARK_RED))
                .addLoreLine(Text.literal("• 🏹 Bone Desperado's Bow").formatted(Formatting.WHITE))
                .addLoreLine(Text.literal("• 🟢 Gelatinous Rustler Boots").formatted(Formatting.GREEN))
                .addLoreLine(Text.literal("• 💀 Sculk Terror Chestplate").formatted(Formatting.DARK_AQUA))
                .addLoreLine(Text.literal("• 👑 Voidwalker's Crown").formatted(Formatting.DARK_PURPLE))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openLegendaryMenu(player))
                .glow()
                .build());

        // 🗡 Special Weapons
        gui.setSlot(24, new GuiElementBuilder(Items.GOLDEN_AXE)
                .setName(Text.literal("🗡️ Special Weapons").formatted(Formatting.YELLOW, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("⚔️ Unique special weapons").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("• 🔨 The Gavel").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("• 🪄 Harvey's Stick").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("• 👟 Hermes Shoes").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("• ⚡ HPEBM (all marks)").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openSpecialWeaponsMenu(player))
                .build());

        // ✦ Boss Drops
        gui.setSlot(29, new GuiElementBuilder(Items.WITHER_SKELETON_SKULL)
                .setName(Text.literal("💀 Boss Drops").formatted(Formatting.DARK_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎁 Unique boss rewards").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("• ❤️ Undead Heart").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("• 📻 Echoing Core").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("• 🌟 Other rare drops").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openBossDropsMenu(player))
                .glow()
                .build());

        // ✦ Materials
        gui.setSlot(30, new GuiElementBuilder(Items.GOLD_BLOCK)
                .setName(Text.literal("🧱 Crafting Materials").formatted(Formatting.AQUA, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🔨 Chunks & Cores for crafting").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("📦 All slayer types available").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("⚡ Used in bounty crafting").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openMaterialsMenu(player))
                .build());

        // 👑 Piglin Items
        gui.setSlot(31, new GuiElementBuilder(Items.GOLDEN_HELMET)
                .setName(Text.literal("👑 Piglin Collection").formatted(Formatting.GOLD, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("💰 Premium piglin items").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("• 👑 Crown of Greed").formatted(Formatting.YELLOW))
                .addLoreLine(Text.literal("• 🌟 Crown of Midas").formatted(Formatting.YELLOW))
                .addLoreLine(Text.literal("• 💎 Piglin Core & Flesh").formatted(Formatting.YELLOW))
                .addLoreLine(Text.literal("• 🛡️ Piglin T1 & T2 Armor").formatted(Formatting.YELLOW))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openPiglinMenu(player))
                .glow()
                .build());

        // ✧ Attribute Tokens
        gui.setSlot(33, new GuiElementBuilder(Items.ENCHANTED_BOOK)
                .setName(Text.literal("✨ Attribute Tokens").formatted(Formatting.LIGHT_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("🎯 All 10 armor attribute tokens").formatted(Formatting.GRAY))
                .addLoreLine(Text.literal("⚡ Apply special abilities to armor").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal("🔥 Burning, Frost, Swift & more!").formatted(Formatting.DARK_GRAY))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("✨ Click to explore").formatted(Formatting.YELLOW))
                .setCallback((idx, type, action) -> openAttributeTokensMenu(player))
                .glow()
                .build());

        // Bottom row navigation
        gui.setSlot(49, new GuiElementBuilder(Items.BARRIER)
                .setName(Text.literal("❌ Close").formatted(Formatting.DARK_RED, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("Exit the gallery").formatted(Formatting.GRAY))
                .setCallback((idx, type, action) -> player.closeHandledScreen())
                .build());

        gui.open();
    }

    // ════════════════════════════════════════════════════════════
    // SWORDS MENU
    // ════════════════════════════════════════════════════════════

    private static void openSwordsMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        for (SlayerManager.SlayerType type : SlayerManager.SlayerType.values()) {
            items.add(SlayerItems.createSlayerSword(type));
        }
        for (SlayerManager.SlayerType type : SlayerManager.SlayerType.values()) {
            items.add(SlayerItems.createUpgradedSlayerSword(type));
        }
        openSubPage(player, "⚔ Bounty Swords", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // ARMOR MENUS (by type with colored glass separators)
    // ════════════════════════════════════════════════════════════

    private static void openArmorMenu(ServerPlayerEntity player, int tier) {
        String title = tier == 1 ? "🛡 Bounty Armor T1" : "🛡 Bounty Armor T2";
        SimpleGui gui = new SimpleGui(ScreenHandlerType.GENERIC_9X6, player, false);
        gui.setTitle(Text.literal("§" + (tier == 1 ? "9" : "5") + title));

        for (int i = 0; i < 54; i++) {
            gui.setSlot(i, new GuiElementBuilder(Items.BLACK_STAINED_GLASS_PANE)
                    .setName(Text.literal("")).build());
        }

        // Header
        gui.setSlot(4, new GuiElementBuilder(tier == 1 ? Items.LEATHER_CHESTPLATE : Items.DIAMOND_CHESTPLATE)
                .setName(Text.literal(title).formatted(tier == 1 ? Formatting.BLUE : Formatting.LIGHT_PURPLE, Formatting.BOLD))
                .addLoreLine(Text.literal(""))
                .addLoreLine(Text.literal("Click a type for its armor pieces").formatted(Formatting.GRAY))
                .build());

        // Slayer type buttons
        SlayerManager.SlayerType[] types = SlayerManager.SlayerType.values();
        int[] typeSlots = {10, 11, 12, 13, 14, 15, 16};

        for (int i = 0; i < types.length && i < typeSlots.length; i++) {
            SlayerManager.SlayerType type = types[i];
            final int finalTier = tier;
            gui.setSlot(typeSlots[i], new GuiElementBuilder(type.icon)
                    .setName(Text.literal(type.displayName + " Armor " + (tier == 1 ? "T1" : "T2"))
                            .formatted(type.color, Formatting.BOLD))
                    .addLoreLine(Text.literal(""))
                    .addLoreLine(Text.literal("4 armor pieces").formatted(Formatting.GRAY))
                    .addLoreLine(Text.literal(""))
                    .addLoreLine(Text.literal("Click to browse").formatted(Formatting.YELLOW))
                    .setCallback((idx, clickType, action) -> openArmorTypeMenu(player, type, finalTier))
                    .build());
        }

        // Back
        gui.setSlot(49, new GuiElementBuilder(Items.ARROW)
                .setName(Text.literal("← Back to Main").formatted(Formatting.GRAY))
                .setCallback((idx, type, action) -> open(player))
                .build());

        gui.open();
    }

    private static void openArmorTypeMenu(ServerPlayerEntity player, SlayerManager.SlayerType type, int tier) {
        List<ItemStack> pieces = new ArrayList<>();
        pieces.add(SlayerItems.createSlayerHelmet(type, tier));
        pieces.add(SlayerItems.createSlayerChestplate(type, tier));
        pieces.add(SlayerItems.createSlayerLeggings(type, tier));
        pieces.add(SlayerItems.createSlayerBoots(type, tier));

        String title = type.displayName + (tier == 1 ? " T1 Armor" : " T2 Armor II");
        openSubPageWithBack(player, title, pieces,
                () -> openArmorMenu(player, tier));
    }

    // ════════════════════════════════════════════════════════════
    // LEGENDARY GEAR MENU
    // ════════════════════════════════════════════════════════════

    private static void openLegendaryMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        items.add(SlayerItems.createZombieBerserkerHelmet());
        items.add(SlayerItems.createSpiderLeggings());
        items.add(SlayerItems.createSkeletonBow());
        items.add(SlayerItems.createSlimeBoots());
        items.add(SlayerItems.createWardenChestplate());
        items.add(SlayerItems.createVoidwalkerCrown());
        openSubPage(player, "⭐ Legendary Gear", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // SPECIAL WEAPONS MENU
    // ════════════════════════════════════════════════════════════

    private static void openSpecialWeaponsMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        items.add(CustomItemHandler.createTheGavel());
        items.add(CustomItemHandler.createHarveysStick());
        items.add(CustomItemHandler.createHermesShoes());
        for (int mk = 1; mk <= 5; mk++) items.add(CustomItemHandler.createHPEBM(mk));
        items.add(CustomItemHandler.createUltraOverclockedBeam());
        openSubPage(player, "🗡 Special Weapons", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // MATERIALS MENU
    // ════════════════════════════════════════════════════════════

    private static void openMaterialsMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        // Add slayer materials
        for (SlayerManager.SlayerType type : SlayerManager.SlayerType.values()) {
            items.add(SlayerItems.createChunk(type));
            items.add(SlayerItems.createCore(type));
        }
        // Add attribute cores
        for (com.political.ArmourAttribute attr : com.political.ArmourAttribute.values()) {
            items.add(SlayerItems.createAttributeCore(attr));
        }
        // Add compacted materials
        items.add(SlayerItems.createCompactedIron());
        items.add(SlayerItems.createCompactedGold());
        items.add(SlayerItems.createCompactedDiamond());
        items.add(SlayerItems.createCompactedEmerald());
        items.add(SlayerItems.createCompactedNetherite());
        items.add(SlayerItems.createCompactedCopper());
        items.add(SlayerItems.createCompactedCoal());
        items.add(SlayerItems.createCompactedLapis());
        items.add(SlayerItems.createCompactedRedstone());
        items.add(SlayerItems.createCompactedQuartz());
        
        // Add enchanted compacted materials
        items.add(SlayerItems.createCompactedIron());
        items.add(SlayerItems.createCompactedGold());
        items.add(SlayerItems.createCompactedDiamond());
        items.add(SlayerItems.createCompactedEmerald());
        items.add(SlayerItems.createCompactedNetherite());
        items.add(SlayerItems.createCompactedCopper());
        items.add(SlayerItems.createCompactedCoal());
        items.add(SlayerItems.createCompactedLapis());
        items.add(SlayerItems.createCompactedRedstone());
        items.add(SlayerItems.createCompactedQuartz());
        
        // Add super compacted ores
        items.add(SlayerItems.createSuperCompactedGold());
        items.add(SlayerItems.createSuperCompactedIron());
        items.add(SlayerItems.createSuperCompactedDiamond());
        items.add(SlayerItems.createSuperCompactedEmerald());
        items.add(SlayerItems.createSuperCompactedNetherite());
        
        // Add enchanted blackstone for gilded armor
        items.add(SlayerItems.createEnchantedBlackstone());
        items.add(SlayerItems.createEnchantedGildedBlackstone());
        
        openSubPage(player, "✦ Materials", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // PIGLIN ITEMS MENU
    // ════════════════════════════════════════════════════════════

    private static void openPiglinMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        items.add(SlayerItems.createCrownOfGreed());
        items.add(SlayerItems.createCrownOfMidas());
        items.add(SlayerItems.createMidasSword());
        items.add(SlayerItems.createPiglinCore());
        items.add(SlayerItems.createPiglinFlesh());
        for (int tier = 1; tier <= 2; tier++) {
            items.add(SlayerItems.createSlayerHelmet(SlayerManager.SlayerType.PIGLIN, tier));
            items.add(SlayerItems.createSlayerChestplate(SlayerManager.SlayerType.PIGLIN, tier));
            items.add(SlayerItems.createSlayerLeggings(SlayerManager.SlayerType.PIGLIN, tier));
            items.add(SlayerItems.createSlayerBoots(SlayerManager.SlayerType.PIGLIN, tier));
        }
        openSubPage(player, "👑 Piglin Items", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // BACKROOMS ITEMS MENU
    // ════════════════════════════════════════════════════════════

    private static void openBossDropsMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        items.add(SlayerItems.createUndeadHeart());
        items.add(SlayerItems.createSpectralQuiver());
        items.add(SlayerItems.createEchoingCore());
        items.add(SlayerItems.createBouncySlime());
        items.add(SlayerItems.createEnderSword());
        items.add(SlayerItems.createAbyssalBlade());
        items.add(SlayerItems.createVenomousDagger());
        items.add(SlayerItems.createTheGavel());
        openSubPage(player, "✦ Boss Drops", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // GENERIC SUB-PAGE (with back to main)
    // ════════════════════════════════════════════════════════════

    private static void openSubPage(ServerPlayerEntity player, String title, List<ItemStack> allItems, int page) {
        openSubPageWithBack(player, title, allItems, page, () -> open(player));
    }

    private static void openSubPageWithBack(ServerPlayerEntity player, String title, List<ItemStack> allItems, Runnable backAction) {
        openSubPageWithBack(player, title, allItems, 0, backAction);
    }

    private static void openSubPageWithBack(ServerPlayerEntity player, String title, List<ItemStack> allItems, int page, Runnable backAction) {
        final int ITEMS_PER_PAGE = 45;
        int totalPages = Math.max(1, (allItems.size() + ITEMS_PER_PAGE - 1) / ITEMS_PER_PAGE);
        if (page < 0) page = 0;
        if (page >= totalPages) page = totalPages - 1;
        final int currentPage = page;

        SimpleGui gui = new SimpleGui(ScreenHandlerType.GENERIC_9X6, player, false);
        gui.setTitle(Text.literal("§6" + title + (totalPages > 1 ? " — " + (page + 1) + "/" + totalPages : "")));

        // Black background on navigation row
        for (int i = 45; i < 54; i++) {
            gui.setSlot(i, new GuiElementBuilder(Items.BLACK_STAINED_GLASS_PANE)
                    .setName(Text.literal("")).build());
        }

        // Previous page
        if (page > 0) {
            gui.setSlot(45, new GuiElementBuilder(Items.ARROW)
                    .setName(Text.literal("← Previous Page").formatted(Formatting.YELLOW))
                    .setCallback((idx, type, action) -> openSubPageWithBack(player, title, allItems, currentPage - 1, backAction))
                    .build());
        }

        // Back to main menu
        gui.setSlot(49, new GuiElementBuilder(Items.ARROW)
                .setName(Text.literal("← Back").formatted(Formatting.GRAY))
                .setCallback((idx, type, action) -> backAction.run())
                .build());

        // Next page
        if (page < totalPages - 1) {
            gui.setSlot(53, new GuiElementBuilder(Items.ARROW)
                    .setName(Text.literal("Next Page →").formatted(Formatting.YELLOW))
                    .setCallback((idx, type, action) -> openSubPageWithBack(player, title, allItems, currentPage + 1, backAction))
                    .build());
        }

        // Fill items
        int start = page * ITEMS_PER_PAGE;
        int end = Math.min(start + ITEMS_PER_PAGE, allItems.size());
        for (int i = start; i < end; i++) {
            ItemStack item = allItems.get(i);
            if (item == null || item.isEmpty()) continue;
            int slot = i - start;
            Text customName = item.get(DataComponentTypes.CUSTOM_NAME);
            final ItemStack finalItem = item.copy();
            
            GuiElementBuilder builder = new GuiElementBuilder(item.getItem())
                    .setName(customName != null ? customName : Text.literal(item.getName().getString()))
                    .addLoreLine(Text.literal("§aClick to receive").formatted(Formatting.GREEN));
            
            // Add the item's lore if it exists
            var loreComponent = item.get(DataComponentTypes.LORE);
            if (loreComponent != null) {
                for (Text loreLine : loreComponent.lines()) {
                    builder.addLoreLine(loreLine);
                }
            }
            
            builder.setCallback((idx, type, action) ->
                    player.getInventory().insertStack(finalItem.copy()));
            
            gui.setSlot(slot, builder.build());
        }

        gui.open();
    }

    // ════════════════════════════════════════════════════════════
    // ATTRIBUTE TOKENS MENU
    // ════════════════════════════════════════════════════════════

    private static void openAttributeTokensMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        // Add all 10 attribute tokens
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.BURNING));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.SIGHTLESS));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.FRENZIED));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.GROUNDED));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.WEBBED));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.FROST));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.PHANTOMSTEP));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.CURSED));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.OVERGROWN));
        items.add(SlayerItems.createAttributeToken(com.political.ArmourAttribute.VOLATILE));
        
        openSubPage(player, "✧ Attribute Tokens", items, 0);
    }

    // ════════════════════════════════════════════════════════════
    // GOLD ARMOR EVOLUTION MENU
    // ════════════════════════════════════════════════════════════

    private static void openGoldArmorMenu(ServerPlayerEntity player) {
        List<ItemStack> items = new ArrayList<>();
        
        // Pure Gold Armor (Tier 1)
        items.add(SlayerItems.createPureGoldHelmet());
        items.add(SlayerItems.createPureGoldChestplate());
        items.add(SlayerItems.createPureGoldLeggings());
        items.add(SlayerItems.createPureGoldBoots());
        
        // Polished Gold Armor (Tier 2)
        items.add(SlayerItems.createPolishedGoldHelmet());
        items.add(SlayerItems.createPolishedGoldChestplate());
        items.add(SlayerItems.createPolishedGoldLeggings());
        items.add(SlayerItems.createPolishedGoldBoots());
        
        // Shiny Gold Armor (Tier 3)
        items.add(SlayerItems.createShinyGoldHelmet());
        items.add(SlayerItems.createShinyGoldChestplate());
        items.add(SlayerItems.createShinyGoldLeggings());
        items.add(SlayerItems.createShinyGoldBoots());
        
        // Glistening Gold Armor (Tier 4)
        items.add(SlayerItems.createGlisteningGoldHelmet());
        items.add(SlayerItems.createGlisteningGoldChestplate());
        items.add(SlayerItems.createGlisteningGoldLeggings());
        items.add(SlayerItems.createGlisteningGoldBoots());
        
        // Gilded Armor (Tier 5)
        items.add(SlayerItems.createGildedHelmet());
        items.add(SlayerItems.createGildedChestplate());
        items.add(SlayerItems.createGildedLeggings());
        items.add(SlayerItems.createGildedBoots());
        
        // Gilded Netherite Armor (Tier 6 - Ultimate)
        items.add(SlayerItems.createGildedNetheriteHelmet());
        items.add(SlayerItems.createGildedNetheriteChestplate());
        items.add(SlayerItems.createGildedNetheriteLeggings());
        items.add(SlayerItems.createGildedNetheriteBoots());
        
        openSubPage(player, "👑 Gold Armor Evolution", items, 0);
    }
}

